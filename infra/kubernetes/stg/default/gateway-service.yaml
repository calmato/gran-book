apiVersion: v1
kind: Service
metadata:
  name: grpc-gateway
  labels:
    api: grpc-gateway
spec:
  selector:
    app: grpc-gateway
  # type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
