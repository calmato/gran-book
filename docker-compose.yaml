version: '3.8'

services:
  # Frontend
  native:
    container_name: gran_book_native
    build:
      context: ./infra/docker/native
      dockerfile: Dockerfile_development
    working_dir: /var/native
    volumes:
      - ./native:/var/native:cached

  admin:
    container_name: gran_book_admin
    build:
      context: ./infra/docker/web/admin
      dockerfile: Dockerfile_development
    working_dir: /var/web/admin
    volumes:
      - ./web/admin:/var/web/admin:cached
    ports:
      - 3000:3000
    environment:
      - FIREBASE_API_KEY=${FIREBASE_API_KEY}
      - FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
      - FIREBASE_MESSAGING_SENDER_ID=${FIREBASE_MESSAGING_SENDER_ID}
    command: yarn dev -H 0.0.0.0 -p 3000

  # Backend
  gateway:
    container_name: gran_book_gateway
    build:
      context: ./infra/docker/infra/gateway
      dockerfile: Dockerfile_development
    working_dir: /go/src/github.com/calmato/gran-book/infra/gateway
    volumes:
      - ./infra/gateway:/go/src/github.com/calmato/gran-book/infra/gateway:cached
      - ./tmp/logs/gateway:/var/log/gateway:delegated
    ports:
      - 8080:8080
    networks:
      - api
    command: realize start

  hello_api:
    container_name: gran_book_hello_api
    build:
      context: ./infra/docker/api/hello
      dockerfile: Dockerfile_development
    working_dir: /go/src/github.com/calmato/gran-book/api/hello
    volumes:
      - ./api/hello:/go/src/github.com/calmato/gran-book/api/hello:cached
    networks:
      - api
    command: go run main.go

  # Monitoring

  # Other
  proto:
    container_name: gran_book_proto
    build:
      context: ./infra/docker/proto
      dockerfile: Dockerfile_development
    working_dir: /go/src/github.com/calmato/gran-book/proto
    volumes:
      - ./proto:/go/src/github.com/calmato/gran-book/proto:cached
      - ./api/hello:/go/src/github.com/calmato/gran-book/api/hello:cached
      - ./docs/12_backend/11_swagger:/go/src/github.com/calmato/gran-book/docs/12_backend/11_swagger:cached
      - ./infra/gateway:/go/src/github.com/calmato/gran-book/infra/gateway:cached

  swagger:
    container_name: gran_book_swagger
    build:
      context: ./infra/docker/infra/swagger
      dockerfile: Dockerfile
    volumes:
      - ./docs/12_backend/11_swagger/apiv1.swagger.json:/var/swagger/apiv1.json:cached
    ports:
      - 8000:8080
    environment:
      SWAGGER_JSON: /var/swagger/apiv1.json

networks:
  api:
    driver: bridge
  monitoring:
    driver: bridge
