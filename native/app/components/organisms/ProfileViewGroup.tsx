import React, { ReactElement } from 'react';
import { View } from 'react-native';

const ProfileViewGroup = function ProfileViewGroup(): ReactElement {
  return (
    <View>
    </View>
  );
};

export default ProfileViewGroup;
