import React, { ReactElement } from 'react';
import { View } from 'react-native';

const ProfileFollowFollwer = function ProfileFollowFollwer(): ReactElement {
  return (
    <View>
    </View>
  );
};

export default ProfileFollowFollwer;
